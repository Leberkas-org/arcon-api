type: object
required:
  - userId
  - email
  - role
  - joinedAt
properties:
  userId:
    type: string
    format: uuid
  email:
    type: string
    format: email
  role:
    type: string
    enum:
      - owner
      - admin
      - member
  joinedAt:
    type: string
    format: date-time
